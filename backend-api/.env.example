# Prisma connection string (constructed from above)
DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}

# ── JWT ──
JWT_SECRET=your_jwt_secret_here
JWT_EXPIRATION=1h
PORT=3000